/**
 * App branding — single source of truth for the user-facing application name.
 *
 * Only display strings should use these constants. Internal identifiers
 * (bundle ID, `TournamentType.ACC` enum, `@acc/*` packages, persisted values)
 * intentionally remain unchanged.
 */

/** Short user-facing app name (was "ACC"). */
export const APP_SHORT_NAME = 'ASC';

/** Full organization name (was "Atmiya Cricket Club"). */
export const APP_ORG_NAME = 'Atmiya Sports Club';
