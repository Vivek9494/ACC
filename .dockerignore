# Dependencies (reinstalled in the image)
node_modules
**/node_modules

# Git / editor
.git
.gitignore
.cursor
**/.DS_Store

# Local env and secrets
.env
.env.*
**/.env
**/.env.*

# Mobile app (not deployed with the API)
apps/mobile

# Local uploads and dev artifacts
apps/api/uploads
**/dist
**/.turbo
**/coverage
**/*.log

# Tests and dev-only tooling
**/*.spec.ts
**/*.test.ts
**/__tests__
**/jest.config.*
**/.jest

# Docker / compose (not needed inside the image)
docker
docker-compose*.yml
Dockerfile*
.dockerignore

# Docs and misc
**/*.md
!apps/api/prisma/**
